package Factory;
/*
* defining a factory method inside an interface.
* the object creation is hidden to the client.
* this can be created as an abstract class as well
* */
public interface Notification {
    String notifyUser();
}
